{
    "annotations": { "list": [] },
    "editable": true,
    "gnetId": null,
    "graphTooltip": 0,
    "panels": [
        {
            "type": "graph",
            "title": "Requests by Result",
            "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 },
            "targets": [{ "expr": "sum by (result) (rate(geofence_requests_total[5m]))", "legendFormat": "{{result}}" }]
        },
        { "type": "stat", "title": "Last Check Inside?", "gridPos": { "h": 4, "w": 6, "x": 0, "y": 8 }, "targets": [{ "expr": "geofence_last_check_status" }] },
        {
            "type": "graph",
            "title": "Request Duration (p95)",
            "gridPos": { "h": 8, "w": 24, "x": 0, "y": 12 },
            "targets": [{ "expr": "histogram_quantile(0.95, sum(rate(geofence_request_duration_seconds_bucket[5m])) by (le))" }]
        }
    ],
    "schemaVersion": 36,
    "title": "Geofence Offer Monitoring",
    "version": 1
}
